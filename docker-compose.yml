services:
  hms:
    build: .
    ports:
      - "8080:5000"
    environment:
      SECRET_KEY: "change-this-secret-key"
      DATABASE_PATH: "/app/data/hotel.db"
      PORT: "5000"
    volumes:
      - hms-data:/app/data

volumes:
  hms-data:
